[project]
name = "nornir"
version = "0.1.0"
description = "Shared contract — data models and constants for the Heimdall system"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
