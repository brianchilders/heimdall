# enrollment — CLI tool to enroll known speakers into memory-mcp.
# Records or loads audio, computes a resemblyzer voiceprint embedding,
# and registers the speaker entity via the memory-mcp voice routes.
